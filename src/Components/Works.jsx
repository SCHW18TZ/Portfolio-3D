import React from "react";
import styled from "styled-components";

const Sectoin = styled.div`
  height: 100vh;
  color: black;
  scroll-snap-align: center;

  background-color: green;
`;

const Works = () => {
  return <Sectoin>Works</Sectoin>;
};

export default Works;
